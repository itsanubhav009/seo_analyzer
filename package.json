{
  "name": "seo-text-optimizer",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "api"],
  "scripts": {
    "build": "npm run build --workspace=client",
    "start": "npm run start --workspace=client",
    "dev": "npm run start --workspace=client"
  },
  "engines": {
    "node": "22.x"
  }
}